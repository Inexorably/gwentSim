#ifndef BOARD_H
#define BOARD_H

#include "gwentCard.h"
#include <vector>

class Board
{
public:
    Board();
    // Each palyer will have a board

    std::vector<GwentCard> melee;
    std::vector<GwentCard> ranged;
    std::vector<GwentCard> siege;
    
    //The graveyard and banished zone.
    std::vector<GwentCard> graveyard;
    std::vector<GwentCard> banished;
};

#endif // BOARD_H